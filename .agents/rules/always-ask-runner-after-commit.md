# Always Ask Runner Policy After Commit

- ALWAYS ask the user via the `ask_question` interactive tool after every git commit whether to launch/run the app or update/restart it on the mobile device if it is already running.
