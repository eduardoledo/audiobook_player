# Always Ask Commit and Push Policy

- ALWAYS ask the user via the `ask_question` interactive tool whether to perform a git commit and push after finishing any implementation, bug fix, or refactoring task.
